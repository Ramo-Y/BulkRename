namespace BulkRename.IntegrationTests.Constants
{
    internal static class ConfigurationConstants
    {
        internal const string ENV_FILE_NAME = ".env";

        internal const string BULK_RENAME_FOLDER = nameof(BULK_RENAME_FOLDER);

        internal const string BULK_RENAME_PORT = nameof(BULK_RENAME_PORT);

        internal const string SUPPORTED_FILE_ENDINGS = nameof(SUPPORTED_FILE_ENDINGS);
    }
}