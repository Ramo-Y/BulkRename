on:
  workflow_call:
    inputs:
      image_version:
        type: string
        required: true
    outputs:
      image_version:
        value: ${{ inputs.image_version }}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v6
    - name: Create temp directories
      run: |
        mkdir -p ${{ runner.temp }}/BulkRename
        mkdir -p ${{ runner.temp }}/BulkRename/Files
        mkdir -p ${{ runner.temp }}/BulkRename/Logs
        mkdir -p ${{ runner.temp }}/BulkRename/RenamingHistory
        chmod -R 777 "${{ runner.temp }}/BulkRename"
    - name: Create Env
      shell: pwsh
      run: ./CreateEnvFile.ps1 -PersistanceMode "Database" -SqlServerSaPassword ${{ secrets.SQL_SERVER_SA_PASSWORD }} -BulkRenameFolder "${{ runner.temp }}/BulkRename/Files" -HistoryFolder "${{ runner.temp }}/BulkRename/RenamingHistory" -LogFolder "${{ runner.temp }}/BulkRename/Logs" -Version ${{ inputs.image_version }}
      working-directory: src
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    - name: Download artifact
      uses: actions/download-artifact@v7
      with:
        name: ${{ vars.IMAGE_NAME }}
        path: /tmp
    - name: Load Docker image
      run: |
        docker load --input /tmp/${{ vars.IMAGE_NAME }}.tar
        docker image ls -a
    - name: Docker Compose
      shell: pwsh
      run: docker compose up -d --wait --wait-timeout 60
      working-directory: src
    - name: Integration Tests
      run: dotnet test src/**/*.IntegrationTests.csproj --logger "trx;LogFileName=integration-tests.trx"
    - name: Upload logs on fail
      if: ${{ failure() }}
      uses: actions/upload-artifact@v6
      with:
        name: Build failure logs
        path: ${{ runner.temp }}/BulkRename/Logs
